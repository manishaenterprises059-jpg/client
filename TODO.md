# TODO

## Theme + Text Fixes (completed)
- [x] Fix dark/light text visibility issues caused by incorrect `.text-white` handling in `src/index.css`.
- [x] Ensure headings/paragraphs use `--text` / `--muted-text` for consistent readability.
- [x] Improve Login page styling to follow theme variables.

## Next
- [ ] Redesign Register page (and optionally Forgot Password) to use theme variables for heading/labels/inputs/buttons so it looks correct in both light and dark modes.


